"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.backwardCompatabilityContextUtils = exports.MINIMUM_SUPPORTED_RELEASE_AFTER_LATEST_CONTEXT_VERSION = exports.LATEST_CONTEXT_VERSION = exports.ContextVersion = void 0;
const shared_1 = require("@activepieces/shared");
var ContextVersion;
(function (ContextVersion) {
    ContextVersion["V1"] = "1";
    ContextVersion["V2"] = "2";
})(ContextVersion || (exports.ContextVersion = ContextVersion = {}));
//bump these two constants after creating a new context version
exports.LATEST_CONTEXT_VERSION = ContextVersion.V2;
exports.MINIMUM_SUPPORTED_RELEASE_AFTER_LATEST_CONTEXT_VERSION = '0.82.0';
exports.backwardCompatabilityContextUtils = {
    makeActionContextBackwardCompatible({ context, contextVersion }) {
        switch (contextVersion) {
            case ContextVersion.V2:
                return context;
            case ContextVersion.V1:
                return addLegacyMethods({ context });
            case undefined:
                return addLegacyMethodsAndServerUrl({ context });
        }
    },
};
function addLegacyMethods({ context }) {
    return Object.assign(Object.assign({}, context), { generateResumeUrl: buildLegacyGenerateResumeUrl({ context }), run: Object.assign(Object.assign({}, context.run), { pause: buildLegacyPauseHook({ context }) }) });
}
function addLegacyMethodsAndServerUrl({ context }) {
    return Object.assign(Object.assign({}, addLegacyMethods({ context })), { serverUrl: context.server.publicUrl });
}
/**
 * @deprecated Since 2026-04-12. Remove after 2026-10-12 once all pieces migrate to createWaitpoint/waitForWaitpoint.
 */
function buildLegacyPauseHook({ context }) {
    return (req) => {
        const type = req.pauseMetadata.type === shared_1.PauseType.DELAY ? 'DELAY' : 'WEBHOOK';
        const responseToSend = req.pauseMetadata.type === shared_1.PauseType.WEBHOOK ? req.pauseMetadata.response : undefined;
        const resumeDateTime = req.pauseMetadata.type === shared_1.PauseType.DELAY ? req.pauseMetadata.resumeDateTime : undefined;
        context.run.createWaitpoint({ type, version: 'V0', resumeDateTime, responseToSend }).catch((e) => {
            console.error('[buildLegacyPauseHook] Failed to create waitpoint', e);
            process.exit(1);
        });
        context.run.waitForWaitpoint('');
    };
}
/**
 * @deprecated Since 2026-04-12. Remove after 2026-10-12 once all pieces migrate to createWaitpoint/waitForWaitpoint.
 */
function buildLegacyGenerateResumeUrl({ context }) {
    return (params) => {
        const randomId = Math.random().toString(36).substring(2);
        const url = new URL(`${context.server.publicUrl}v1/flow-runs/${context.run.id}/requests/${randomId}${params.sync ? '/sync' : ''}`);
        url.search = new URLSearchParams(params.queryParams).toString();
        return url.toString();
    };
}
//# sourceMappingURL=versioning.js.map