"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GmailRequests = void 0;
exports.parseStream = parseStream;
exports.convertAttachment = convertAttachment;
exports.getFirstFiveOrAll = getFirstFiveOrAll;
const tslib_1 = require("tslib");
const models_1 = require("./models");
const pieces_common_1 = require("@activepieces/pieces-common");
const mailparser_1 = require("mailparser");
const auth_1 = require("../auth");
exports.GmailRequests = {
    getMail: (_a) => tslib_1.__awaiter(void 0, [_a], void 0, function* ({ access_token, format, message_id }) {
        const response = yield pieces_common_1.httpClient.sendRequest({
            method: pieces_common_1.HttpMethod.GET,
            url: `https://gmail.googleapis.com/gmail/v1/users/me/messages/${message_id}`,
            authentication: {
                type: pieces_common_1.AuthenticationType.BEARER_TOKEN,
                token: access_token,
            },
            queryParams: {
                format,
            },
        });
        let bodyHtml = '';
        let bodyPlain = '';
        const payload = response.body.payload;
        let bodyParts = payload.parts || [];
        const headers = payload.headers.reduce((obj, header) => {
            obj[header.name.toLowerCase()] = header.value;
            return obj;
        }, {});
        const alternateBodyPart = bodyParts.find((part) => part.mimeType.startsWith('multipart/alternative'));
        if (alternateBodyPart && (alternateBodyPart === null || alternateBodyPart === void 0 ? void 0 : alternateBodyPart.parts)) {
            bodyParts = alternateBodyPart.parts;
        }
        const subject = headers['subject'] || '';
        // Determine the content type of the message body
        const contentType = headers['content-type'] || 'text/plain';
        const isMultipart = contentType.startsWith('multipart/');
        if (isMultipart) {
            // If the message is multipart, extract the plain text and HTML parts
            const textPart = bodyParts.find((part) => part.mimeType === 'text/plain');
            const htmlPart = bodyParts.find((part) => part.mimeType === 'text/html');
            // If the message is an "alternative" multipart, use the plain text part if it exists
            const preferredPart = textPart || htmlPart;
            bodyHtml = htmlPart ? decodeBase64(htmlPart.body.data) : '';
            bodyPlain = preferredPart ? decodeBase64(preferredPart.body.data) : '';
        }
        else {
            // If the message is not multipart, use the body as-is
            bodyPlain = decodeBase64(payload.body.data);
            bodyHtml = '';
        }
        return Object.assign({ subject: subject, body_html: bodyHtml, body_plain: bodyPlain }, response.body);
    }),
    getThread: (_a) => tslib_1.__awaiter(void 0, [_a], void 0, function* ({ access_token, format, thread_id }) {
        const response = yield pieces_common_1.httpClient.sendRequest({
            method: pieces_common_1.HttpMethod.GET,
            url: `https://gmail.googleapis.com/gmail/v1/users/me/threads/${thread_id}`,
            authentication: {
                type: pieces_common_1.AuthenticationType.BEARER_TOKEN,
                token: access_token,
            },
            queryParams: {
                format,
            },
        });
        return response.body;
    }),
    getLabels: (authentication) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
        return yield pieces_common_1.httpClient.sendRequest({
            method: pieces_common_1.HttpMethod.GET,
            url: `https://gmail.googleapis.com/gmail/v1/users/me/labels`,
            authentication: {
                type: pieces_common_1.AuthenticationType.BEARER_TOKEN,
                token: yield (0, auth_1.getAccessToken)(authentication),
            },
        });
    }),
    searchMail: (_a) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
        var { access_token, max_results = 1, page_token: pageToken } = _a, mail = tslib_1.__rest(_a, ["access_token", "max_results", "page_token"]);
        const query = [];
        if (mail.from)
            query.push(`from:(${mail.from})`);
        if (mail.to)
            query.push(`to:(${mail.to})`);
        if (mail.subject)
            query.push(`subject:(${mail.subject})`);
        if (mail.label)
            query.push(`label:${mail.label.name}`);
        if (mail.category)
            query.push(`category:${mail.category}`);
        if (mail.after != null && mail.after > 0)
            query.push(`after:${mail.after}`);
        if (mail.before != null)
            query.push(`before:${mail.before}`);
        const response = yield pieces_common_1.httpClient.sendRequest({
            method: pieces_common_1.HttpMethod.GET,
            url: `https://www.googleapis.com/gmail/v1/users/me/messages`,
            authentication: {
                type: pieces_common_1.AuthenticationType.BEARER_TOKEN,
                token: access_token,
            },
            queryParams: Object.assign({ q: query.join(' '), maxResults: `${max_results}` }, (pageToken ? { pageToken } : {})),
        });
        if (response.body.messages) {
            const messages = yield Promise.all(response.body.messages.map((message) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
                const mail = yield exports.GmailRequests.getMail({
                    access_token,
                    message_id: message.id,
                    format: models_1.GmailMessageFormat.FULL,
                });
                const thread = yield exports.GmailRequests.getThread({
                    access_token,
                    thread_id: message.threadId,
                    format: models_1.GmailMessageFormat.FULL,
                });
                return {
                    message: mail,
                    thread,
                };
            })));
            return Object.assign({ messages, resultSizeEstimate: response.body.resultSizeEstimate }, ((response === null || response === void 0 ? void 0 : response.body.nextPageToken)
                ? { nextPageToken: response.body.nextPageToken }
                : {}));
        }
        return response.body;
    }),
    getRecentMessages: (authentication_1, ...args_1) => tslib_1.__awaiter(void 0, [authentication_1, ...args_1], void 0, function* (authentication, maxResults = 20) {
        return yield pieces_common_1.httpClient.sendRequest({
            method: pieces_common_1.HttpMethod.GET,
            url: 'https://gmail.googleapis.com/gmail/v1/users/me/messages',
            authentication: {
                type: pieces_common_1.AuthenticationType.BEARER_TOKEN,
                token: yield (0, auth_1.getAccessToken)(authentication),
            },
            queryParams: {
                maxResults: maxResults.toString(),
                q: 'in:inbox OR in:sent', // Get recent messages from inbox and sent
            },
        });
    }),
    getRecentThreads: (authentication_1, ...args_1) => tslib_1.__awaiter(void 0, [authentication_1, ...args_1], void 0, function* (authentication, maxResults = 15) {
        return yield pieces_common_1.httpClient.sendRequest({
            method: pieces_common_1.HttpMethod.GET,
            url: 'https://gmail.googleapis.com/gmail/v1/users/me/threads',
            authentication: {
                type: pieces_common_1.AuthenticationType.BEARER_TOKEN,
                token: yield (0, auth_1.getAccessToken)(authentication),
            },
            queryParams: {
                maxResults: maxResults.toString(),
                q: 'in:inbox OR in:sent', // Get recent threads from inbox and sent
            },
        });
    }),
};
function decodeBase64(data) {
    return Buffer.from(data, 'base64').toString();
}
function parseStream(stream) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        return new Promise((resolve, reject) => {
            (0, mailparser_1.simpleParser)(stream, (err, parsed) => {
                if (err) {
                    reject(err);
                }
                else {
                    resolve(parsed);
                }
            });
        });
    });
}
function convertAttachment(attachments, files) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        const promises = attachments.map((attachment) => tslib_1.__awaiter(this, void 0, void 0, function* () {
            var _a;
            try {
                const fileName = (_a = attachment.filename) !== null && _a !== void 0 ? _a : `attachment-${Date.now()}`;
                return {
                    fileName,
                    mimeType: attachment.contentType,
                    size: attachment.size,
                    data: yield files.write({
                        fileName: fileName,
                        data: attachment.content,
                    }),
                };
            }
            catch (error) {
                console.error(`Failed to process attachment: ${attachment.filename}`, error);
                return null;
            }
        }));
        const results = yield Promise.all(promises);
        return results.filter((result) => result !== null);
    });
}
function getFirstFiveOrAll(array) {
    if (array.length <= 5) {
        return array;
    }
    else {
        return array.slice(0, 5);
    }
}
//# sourceMappingURL=data.js.map