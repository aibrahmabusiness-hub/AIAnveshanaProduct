"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.flowCanvasUtils = exports.FLOW_CANVAS_HSPACE = exports.FLOW_CANVAS_ROUTER_VOFFSET = exports.FLOW_CANVAS_LOOP_VOFFSET = exports.FLOW_CANVAS_ARC = exports.FLOW_CANVAS_VSPACE = exports.FLOW_CANVAS_STEP_WIDTH = exports.FLOW_CANVAS_STEP_HEIGHT = void 0;
const action_1 = require("../actions/action");
exports.FLOW_CANVAS_STEP_HEIGHT = 60;
exports.FLOW_CANVAS_STEP_WIDTH = 232;
exports.FLOW_CANVAS_VSPACE = 60;
exports.FLOW_CANVAS_ARC = 15;
exports.FLOW_CANVAS_LOOP_VOFFSET = exports.FLOW_CANVAS_VSPACE * 1.5 + 2 * exports.FLOW_CANVAS_ARC; // 120
exports.FLOW_CANVAS_ROUTER_VOFFSET = exports.FLOW_CANVAS_LOOP_VOFFSET + 30; // 150
exports.FLOW_CANVAS_HSPACE = 80;
function getFlowBBox(step, forBranch = false) {
    if (!step) {
        return forBranch
            ? { minX: 0, maxX: exports.FLOW_CANVAS_STEP_WIDTH, height: exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_VSPACE }
            : { minX: 0, maxX: exports.FLOW_CANVAS_STEP_WIDTH, height: 0 };
    }
    let withChildMinX = 0;
    let withChildMaxX = exports.FLOW_CANVAS_STEP_WIDTH;
    let withChildHeight = exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_VSPACE;
    if (step.type === action_1.FlowActionType.LOOP_ON_ITEMS) {
        const loopStep = step;
        const childBBox = getFlowBBox(loopStep.firstLoopAction, true);
        const childWidth = childBBox.maxX - childBBox.minX;
        const childLeft = -childBBox.minX + exports.FLOW_CANVAS_STEP_WIDTH / 2;
        const childRight = childBBox.maxX - exports.FLOW_CANVAS_STEP_WIDTH / 2;
        const deltaLeftX = -(childWidth + exports.FLOW_CANVAS_STEP_WIDTH + exports.FLOW_CANVAS_HSPACE - exports.FLOW_CANVAS_STEP_WIDTH / 2 - childRight) / 2 - exports.FLOW_CANVAS_STEP_WIDTH / 2;
        const childOffsetX = deltaLeftX + exports.FLOW_CANVAS_STEP_WIDTH + exports.FLOW_CANVAS_HSPACE + childLeft;
        const subgraphEndY = exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_LOOP_VOFFSET + childBBox.height + exports.FLOW_CANVAS_ARC + exports.FLOW_CANVAS_VSPACE;
        withChildMinX = Math.min(0, deltaLeftX, childOffsetX + childBBox.minX);
        withChildMaxX = Math.max(exports.FLOW_CANVAS_STEP_WIDTH, deltaLeftX + exports.FLOW_CANVAS_STEP_WIDTH, childOffsetX + childBBox.maxX);
        withChildHeight = Math.max(exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_VSPACE, subgraphEndY);
    }
    else if (step.type === action_1.FlowActionType.ROUTER) {
        const routerStep = step;
        const children = routerStep.children;
        if (children.length > 0) {
            const childBBoxes = children.map(c => getFlowBBox(c, true));
            const totalWidth = childBBoxes.reduce((sum, b) => sum + (b.maxX - b.minX), 0) + exports.FLOW_CANVAS_HSPACE * (children.length - 1);
            const firstLeft = -childBBoxes[0].minX + exports.FLOW_CANVAS_STEP_WIDTH / 2;
            const lastRight = childBBoxes[children.length - 1].maxX - exports.FLOW_CANVAS_STEP_WIDTH / 2;
            let deltaLeftX = -(totalWidth - firstLeft - lastRight) / 2 - firstLeft;
            let routerMinX = 0;
            let routerMaxX = exports.FLOW_CANVAS_STEP_WIDTH;
            let maxChildHeight = 0;
            for (let i = 0; i < children.length; i++) {
                const bbox = childBBoxes[i];
                const x = deltaLeftX + (-bbox.minX + exports.FLOW_CANVAS_STEP_WIDTH / 2);
                routerMinX = Math.min(routerMinX, x + bbox.minX);
                routerMaxX = Math.max(routerMaxX, x + bbox.maxX);
                maxChildHeight = Math.max(maxChildHeight, bbox.height);
                deltaLeftX += (bbox.maxX - bbox.minX) + exports.FLOW_CANVAS_HSPACE;
            }
            const subgraphEndY = exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_ROUTER_VOFFSET + maxChildHeight + exports.FLOW_CANVAS_ARC + exports.FLOW_CANVAS_VSPACE;
            withChildMinX = routerMinX;
            withChildMaxX = routerMaxX;
            withChildHeight = Math.max(exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_VSPACE, subgraphEndY);
        }
    }
    const nextBBox = getFlowBBox(step.nextAction, false);
    return {
        minX: Math.min(withChildMinX, nextBBox.minX),
        maxX: Math.max(withChildMaxX, nextBBox.maxX),
        height: withChildHeight + nextBBox.height,
    };
}
function buildPositions(step, offsetX, offsetY, positions) {
    if (!step)
        return;
    positions.set(step.name, { x: offsetX + exports.FLOW_CANVAS_STEP_WIDTH / 2, y: offsetY });
    if (step.type === action_1.FlowActionType.LOOP_ON_ITEMS) {
        const loopStep = step;
        const childBBox = getFlowBBox(loopStep.firstLoopAction, true);
        const childLeft = -childBBox.minX + exports.FLOW_CANVAS_STEP_WIDTH / 2;
        const childRight = childBBox.maxX - exports.FLOW_CANVAS_STEP_WIDTH / 2;
        const childWidth = childBBox.maxX - childBBox.minX;
        const deltaLeftX = -(childWidth + exports.FLOW_CANVAS_STEP_WIDTH + exports.FLOW_CANVAS_HSPACE - exports.FLOW_CANVAS_STEP_WIDTH / 2 - childRight) / 2 - exports.FLOW_CANVAS_STEP_WIDTH / 2;
        const childOffsetX = offsetX + deltaLeftX + exports.FLOW_CANVAS_STEP_WIDTH + exports.FLOW_CANVAS_HSPACE + childLeft;
        const childOffsetY = offsetY + exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_LOOP_VOFFSET;
        if (loopStep.firstLoopAction) {
            buildPositions(loopStep.firstLoopAction, childOffsetX, childOffsetY, positions);
        }
        const subgraphEndY = exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_LOOP_VOFFSET + childBBox.height + exports.FLOW_CANVAS_ARC + exports.FLOW_CANVAS_VSPACE;
        buildPositions(step.nextAction, offsetX, offsetY + subgraphEndY, positions);
    }
    else if (step.type === action_1.FlowActionType.ROUTER) {
        const routerStep = step;
        const children = routerStep.children;
        const childBBoxes = children.map(c => getFlowBBox(c, true));
        let maxChildHeight = 0;
        if (children.length > 0) {
            const totalWidth = childBBoxes.reduce((sum, b) => sum + (b.maxX - b.minX), 0) + exports.FLOW_CANVAS_HSPACE * (children.length - 1);
            const firstLeft = -childBBoxes[0].minX + exports.FLOW_CANVAS_STEP_WIDTH / 2;
            const lastRight = childBBoxes[children.length - 1].maxX - exports.FLOW_CANVAS_STEP_WIDTH / 2;
            let deltaLeftX = -(totalWidth - firstLeft - lastRight) / 2 - firstLeft;
            const childOffsetY = offsetY + exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_ROUTER_VOFFSET;
            for (let i = 0; i < children.length; i++) {
                const child = children[i];
                const bbox = childBBoxes[i];
                const branchOffsetX = offsetX + deltaLeftX + (-bbox.minX + exports.FLOW_CANVAS_STEP_WIDTH / 2);
                if (child) {
                    buildPositions(child, branchOffsetX, childOffsetY, positions);
                }
                maxChildHeight = Math.max(maxChildHeight, bbox.height);
                deltaLeftX += (bbox.maxX - bbox.minX) + exports.FLOW_CANVAS_HSPACE;
            }
        }
        const subgraphEndY = exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_ROUTER_VOFFSET + maxChildHeight + exports.FLOW_CANVAS_ARC + exports.FLOW_CANVAS_VSPACE;
        buildPositions(step.nextAction, offsetX, offsetY + subgraphEndY, positions);
    }
    else {
        buildPositions(step.nextAction, offsetX, offsetY + exports.FLOW_CANVAS_STEP_HEIGHT + exports.FLOW_CANVAS_VSPACE, positions);
    }
}
exports.flowCanvasUtils = {
    /**
     * Compute canvas (x, y) positions for every step in a flow.
     * Positions match the frontend canvas layout algorithm.
     */
    computeStepPositions(trigger) {
        const positions = new Map();
        buildPositions(trigger, 0, 0, positions);
        return positions;
    },
};
//# sourceMappingURL=flow-canvas-util.js.map