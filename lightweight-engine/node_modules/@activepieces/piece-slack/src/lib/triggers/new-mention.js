"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.newMention = void 0;
const tslib_1 = require("tslib");
const pieces_framework_1 = require("@activepieces/pieces-framework");
const props_1 = require("../common/props");
const auth_1 = require("../auth");
const auth_helpers_1 = require("../common/auth-helpers");
exports.newMention = (0, pieces_framework_1.createTrigger)({
    auth: auth_1.slackAuth,
    name: 'new_mention',
    displayName: 'New Mention in Channel',
    description: 'Triggers when a user or user group is mentioned.',
    props: {
        info: props_1.multiSelectChannelInfo,
        users: props_1.userIds,
        usergroups: props_1.usergroupIds,
        channels: pieces_framework_1.Property.MultiSelectDropdown({
            auth: auth_1.slackAuth,
            displayName: 'Channels',
            description: 'If no channel is selected, the flow will be triggered for username mentions in all channels',
            required: false,
            refreshers: [],
            options(_a) {
                return tslib_1.__awaiter(this, arguments, void 0, function* ({ auth }) {
                    if (!auth) {
                        return {
                            disabled: true,
                            placeholder: 'connect slack account',
                            options: [],
                        };
                    }
                    const accessToken = (0, auth_helpers_1.getBotToken)(auth);
                    const channels = yield (0, props_1.getChannels)(accessToken);
                    return {
                        disabled: false,
                        placeholder: 'Select channel',
                        options: channels,
                    };
                });
            },
        }),
        ignoreBots: pieces_framework_1.Property.Checkbox({
            displayName: 'Ignore Bot Messages ?',
            required: true,
            defaultValue: false,
        }),
        removeMention: pieces_framework_1.Property.Checkbox({
            displayName: 'Remove Mention from Message',
            description: 'If enabled, provides a clean_text field with the user and user group mentions removed from the message.',
            required: true,
            defaultValue: false,
        }),
    },
    type: pieces_framework_1.TriggerStrategy.APP_WEBHOOK,
    sampleData: undefined,
    onEnable: (context) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
        const teamId = yield (0, auth_helpers_1.getTeamId)(context.auth);
        context.app.createListeners({
            events: ['message'],
            identifierValue: teamId,
        });
    }),
    onDisable: (context) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
        // Ignored
    }),
    run: (context) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
        var _a, _b, _c, _d;
        const payloadBody = context.payload.body;
        const channels = (_a = context.propsValue.channels) !== null && _a !== void 0 ? _a : [];
        const users = (_b = context.propsValue.users) !== null && _b !== void 0 ? _b : [];
        const usergroups = (_c = context.propsValue.usergroups) !== null && _c !== void 0 ? _c : [];
        // check if it's channel message
        if (!['channel', 'group'].includes(payloadBody.event.channel_type)) {
            return [];
        }
        if (channels.length > 0 && !channels.includes(payloadBody.event.channel)) {
            return [];
        }
        // check for bot messages
        if (context.propsValue.ignoreBots && payloadBody.event.bot_id) {
            return [];
        }
        const text = (_d = payloadBody.event.text) !== null && _d !== void 0 ? _d : '';
        const userMentioned = users.some((uid) => text.includes(`<@${uid}>`));
        const usergroupMentioned = usergroups.some((gid) => new RegExp(`<!subteam\\^${gid}(\\|[^>]*)?>`).test(text));
        if (!userMentioned && !usergroupMentioned) {
            return [];
        }
        if (context.propsValue.removeMention) {
            let cleanText = text;
            for (const uid of users) {
                cleanText = cleanText.replace(new RegExp(`<@${uid}>`, 'g'), '');
            }
            for (const gid of usergroups) {
                cleanText = cleanText.replace(new RegExp(`<!subteam\\^${gid}(\\|[^>]*)?>`, 'g'), '');
            }
            cleanText = cleanText.trim();
            return [Object.assign(Object.assign({}, payloadBody.event), { clean_text: cleanText })];
        }
        return [payloadBody.event];
    }),
});
//# sourceMappingURL=new-mention.js.map