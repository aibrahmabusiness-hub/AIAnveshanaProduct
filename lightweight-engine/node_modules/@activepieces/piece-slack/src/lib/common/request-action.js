"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestAction = void 0;
const tslib_1 = require("tslib");
const utils_1 = require("./utils");
const shared_1 = require("@activepieces/shared");
const auth_helpers_1 = require("./auth-helpers");
const requestAction = (conversationId, context) => tslib_1.__awaiter(void 0, void 0, void 0, function* () {
    var _a, _b, _c;
    const { actions } = context.propsValue;
    (0, shared_1.assertNotNullOrUndefined)(actions, 'actions');
    if (!actions.length) {
        throw new Error(`Must have at least one button action`);
    }
    const actionTextToIds = actions.map(({ label, style }) => {
        if (!label) {
            throw new Error(`Button text for the action cannot be empty`);
        }
        return {
            label,
            style,
            actionId: encodeURI(label),
        };
    });
    if (context.executionType === shared_1.ExecutionType.BEGIN) {
        const waitpoint = yield context.run.createWaitpoint({
            type: 'WEBHOOK',
        });
        const token = (0, auth_helpers_1.getBotToken)(context.auth);
        const { text, username, profilePicture, replyBroadcast } = context.propsValue;
        (0, shared_1.assertNotNullOrUndefined)(token, 'token');
        (0, shared_1.assertNotNullOrUndefined)(text, 'text');
        const actionElements = actionTextToIds.map((action) => {
            const actionLink = waitpoint.buildResumeUrl({
                queryParams: { action: action.actionId },
            });
            return Object.assign(Object.assign({ type: 'button', text: {
                    type: 'plain_text',
                    text: action.label,
                } }, (action.style && { style: action.style })), { value: actionLink, action_id: action.actionId });
        });
        const messageResponse = yield (0, utils_1.slackSendMessage)({
            token,
            text: `${context.propsValue.text}`,
            username,
            profilePicture,
            threadTs: context.propsValue.threadTs
                ? (0, utils_1.processMessageTimestamp)(context.propsValue.threadTs)
                : undefined,
            replyBroadcast,
            blocks: [
                ...(0, utils_1.textToSectionBlocks)(`${context.propsValue.text}`),
                {
                    type: 'actions',
                    block_id: 'actions',
                    elements: actionElements,
                },
                ...(context.propsValue.mentionOriginFlow
                    ? [(0, utils_1.buildFlowOriginContextBlock)(context)]
                    : []),
            ],
            conversationId: conversationId,
        });
        context.run.waitForWaitpoint(waitpoint.id);
        return {
            action: actionTextToIds.at(0) || 'N/A',
            payload: {
                type: 'action_blocks',
                user: {
                    id: (_a = messageResponse.message) === null || _a === void 0 ? void 0 : _a.user,
                    username: 'user.name',
                    name: 'john.smith',
                    team_id: (_b = messageResponse.message) === null || _b === void 0 ? void 0 : _b.team,
                },
                container: {
                    type: 'message',
                    message_ts: messageResponse.ts,
                    channel_id: messageResponse.channel,
                    is_ephemeral: false,
                },
                trigger_id: 'trigger_id',
                team: {
                    id: (_c = messageResponse.message) === null || _c === void 0 ? void 0 : _c.team,
                    domain: 'team_name',
                },
                channel: {
                    id: messageResponse.channel,
                    name: '#channel',
                },
                message: messageResponse.message,
                state: {},
                actions: [
                    {
                        action_id: 'action_id',
                        block_id: 'actions',
                        value: 'resume_url',
                        style: 'primary',
                        type: 'button',
                        action_ts: 'action_ts',
                    },
                ],
            },
        };
    }
    else {
        const payloadQueryParams = context.resumePayload.queryParams;
        return {
            action: decodeURI(payloadQueryParams.action),
            payload: context.resumePayload.body,
        };
    }
});
exports.requestAction = requestAction;
//# sourceMappingURL=request-action.js.map