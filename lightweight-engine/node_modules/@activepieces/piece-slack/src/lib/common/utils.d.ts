import { ApFile } from '@activepieces/pieces-framework';
import { Block, KnownBlock } from '@slack/web-api';
export declare function textToSectionBlocks(text: string): (KnownBlock | Block)[];
export declare function buildFlowOriginContextBlock(context: {
    server: {
        publicUrl: string;
    };
    project: {
        id: string;
    };
    flows: {
        current: {
            id: string;
        };
    };
}): KnownBlock;
export declare const slackSendMessage: ({ text, conversationId, username, profilePicture, iconEmoji, blocks, threadTs, token, file, replyBroadcast, unfurlLinks, }: SlackSendMessageParams) => Promise<import("@slack/web-api").WebAPICallResult>;
type SlackSendMessageParams = {
    token: string;
    conversationId: string;
    username?: string;
    profilePicture?: string;
    iconEmoji?: string;
    blocks?: unknown[] | Record<string, any>;
    text?: string;
    file?: ApFile;
    threadTs?: string;
    replyBroadcast?: boolean;
    unfurlLinks?: boolean;
};
export declare function processMessageTimestamp(input: string): string | undefined;
export declare function getFirstFiveOrAll(array: unknown[]): unknown[];
/**
* Parse a message text to extract command and arguments
*/
export declare function parseCommand(text: string, botUserId: string, validCommands: string[]): {
    command: string;
    args: string[];
} | null;
export {};
//# sourceMappingURL=utils.d.ts.map