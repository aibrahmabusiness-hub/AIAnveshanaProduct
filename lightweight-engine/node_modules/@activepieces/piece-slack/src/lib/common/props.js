"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.actions = exports.text = exports.usergroupIds = exports.userIds = exports.userId = exports.blocks = exports.mentionOriginFlow = exports.threadTs = exports.iconEmoji = exports.profilePicture = exports.username = exports.slackChannel = exports.singleSelectChannelInfo = exports.multiSelectChannelInfo = void 0;
exports.getChannels = getChannels;
const tslib_1 = require("tslib");
const pieces_framework_1 = require("@activepieces/pieces-framework");
const web_api_1 = require("@slack/web-api");
const auth_1 = require("../auth");
const auth_helpers_1 = require("../common/auth-helpers");
const slackChannelBotInstruction = `
	Please make sure add the bot to the channel by following these steps:
	  1. Type /invite in the channel's chat.
	  2. Click on Add apps to this channel.
	  3. Search for and add the bot.
  `;
exports.multiSelectChannelInfo = pieces_framework_1.Property.MarkDown({
    value: slackChannelBotInstruction +
        `\n**Note**: If you can't find the channel in the dropdown list (which fetches up to 2000 channels), please click on the **(F)** and type the channel ID directly in an array like this: \`{\`{ ['your_channel_id_1', 'your_channel_id_2', ...] \`}\`}`,
});
exports.singleSelectChannelInfo = pieces_framework_1.Property.MarkDown({
    value: slackChannelBotInstruction +
        `\n**Note**: If you can't find the channel in the dropdown list (which fetches up to 2000 channels), please click on the **(F)** and type the channel ID directly.
  `,
});
const slackChannel = (required) => pieces_framework_1.Property.Dropdown({
    auth: auth_1.slackAuth,
    displayName: 'Channel',
    description: "You can get the Channel ID by right-clicking on the channel and selecting 'View Channel Details.'",
    required,
    refreshers: [],
    options(_a) {
        return tslib_1.__awaiter(this, arguments, void 0, function* ({ auth }) {
            if (!auth) {
                return {
                    disabled: true,
                    placeholder: 'connect slack account',
                    options: [],
                };
            }
            const accessToken = (0, auth_helpers_1.getBotToken)(auth);
            const channels = yield getChannels(accessToken);
            return {
                disabled: false,
                placeholder: 'Select channel',
                options: channels,
            };
        });
    },
});
exports.slackChannel = slackChannel;
exports.username = pieces_framework_1.Property.ShortText({
    displayName: 'Username',
    description: 'The username of the bot',
    required: false,
});
exports.profilePicture = pieces_framework_1.Property.ShortText({
    displayName: 'Profile Picture',
    description: 'The profile picture of the bot',
    required: false,
});
exports.iconEmoji = pieces_framework_1.Property.ShortText({
    displayName: 'Icon Emoji',
    description: 'The icon emoji of the bot',
    required: false,
});
exports.threadTs = pieces_framework_1.Property.ShortText({
    displayName: 'Reply to Thread (Thread Message Link/Timestamp)',
    description: 'Provide the ts (timestamp) or link value of the **parent** message to make this message a reply. Do not use the ts value of the reply itself; use its parent instead. For example `1710304378.475129`.Alternatively, you can easily obtain the message link by clicking on the three dots next to the parent message and selecting the `Copy link` option.',
    required: false,
});
exports.mentionOriginFlow = pieces_framework_1.Property.Checkbox({
    displayName: 'Mention flow of origin?',
    description: 'If checked, adds a mention at the end of the Slack message to indicate which flow sent the notification, with a link to said flow.',
    required: false,
    defaultValue: false,
});
exports.blocks = pieces_framework_1.Property.Json({
    displayName: 'Block Kit blocks',
    description: 'See https://api.slack.com/block-kit for specs',
    required: false,
    defaultValue: []
});
const userId = (required) => pieces_framework_1.Property.Dropdown({
    auth: auth_1.slackAuth,
    displayName: 'User',
    required,
    refreshers: [],
    options(_a) {
        return tslib_1.__awaiter(this, arguments, void 0, function* ({ auth }) {
            if (!auth) {
                return {
                    disabled: true,
                    placeholder: 'connect slack account',
                    options: [],
                };
            }
            const accessToken = (0, auth_helpers_1.getBotToken)(auth);
            const users = yield getUsers(accessToken);
            return {
                disabled: false,
                placeholder: 'Select User',
                options: users,
            };
        });
    },
});
exports.userId = userId;
exports.userIds = pieces_framework_1.Property.MultiSelectDropdown({
    auth: auth_1.slackAuth,
    displayName: 'Users',
    required: false,
    refreshers: [],
    options(_a) {
        return tslib_1.__awaiter(this, arguments, void 0, function* ({ auth }) {
            if (!auth) {
                return {
                    disabled: true,
                    placeholder: 'connect slack account',
                    options: [],
                };
            }
            const accessToken = (0, auth_helpers_1.getBotToken)(auth);
            const users = yield getUsers(accessToken);
            return {
                disabled: false,
                placeholder: 'Select Users',
                options: users,
            };
        });
    },
});
exports.usergroupIds = pieces_framework_1.Property.MultiSelectDropdown({
    auth: auth_1.slackAuth,
    displayName: 'User Groups',
    required: false,
    refreshers: [],
    options(_a) {
        return tslib_1.__awaiter(this, arguments, void 0, function* ({ auth }) {
            var _b;
            if (!auth) {
                return {
                    disabled: true,
                    placeholder: 'connect slack account',
                    options: [],
                };
            }
            const accessToken = (0, auth_helpers_1.getBotToken)(auth);
            const client = new web_api_1.WebClient(accessToken);
            const response = yield client.usergroups.list();
            const usergroups = ((_b = response.usergroups) !== null && _b !== void 0 ? _b : [])
                .filter((ug) => !ug.date_delete)
                .map((ug) => ({
                label: ug.handle || ug.name || '',
                value: ug.id || '',
            }));
            return {
                disabled: false,
                placeholder: 'Select User Groups',
                options: usergroups,
            };
        });
    },
});
exports.text = pieces_framework_1.Property.LongText({
    displayName: 'Message',
    required: true,
});
exports.actions = pieces_framework_1.Property.Array({
    displayName: 'Action Buttons',
    required: true,
    properties: {
        label: pieces_framework_1.Property.ShortText({
            displayName: 'Label',
            required: true,
        }),
        style: pieces_framework_1.Property.StaticDropdown({
            displayName: 'Style',
            required: false,
            defaultValue: null,
            options: {
                options: [
                    { label: 'Default', value: null },
                    { label: 'Primary', value: 'primary' },
                    { label: 'Danger', value: 'danger' },
                ],
            },
        }),
    },
});
function getUsers(accessToken) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        var _a, e_1, _b, _c;
        const client = new web_api_1.WebClient(accessToken);
        const users = [];
        try {
            for (var _d = true, _e = tslib_1.__asyncValues(client.paginate('users.list', {
                limit: 1000,
            })), _f; _f = yield _e.next(), _a = _f.done, !_a; _d = true) {
                _c = _f.value;
                _d = false;
                const page = _c;
                const response = page;
                if (response.members) {
                    users.push(...response.members
                        .filter((member) => !member.deleted)
                        .map((member) => ({
                        label: member.name || '',
                        value: member.id || '',
                    })));
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (!_d && !_a && (_b = _e.return)) yield _b.call(_e);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return users;
    });
}
function getChannels(accessToken) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        var _a;
        const client = new web_api_1.WebClient(accessToken);
        const channels = [];
        const CHANNELS_LIMIT = 2000;
        let cursor;
        do {
            const response = yield client.conversations.list({
                types: 'public_channel,private_channel',
                exclude_archived: true,
                limit: 1000,
                cursor,
            });
            if (response.channels) {
                channels.push(...response.channels.map((channel) => {
                    return { label: channel.name || '', value: channel.id || '' };
                }));
            }
            cursor = (_a = response.response_metadata) === null || _a === void 0 ? void 0 : _a.next_cursor;
        } while (cursor && channels.length < CHANNELS_LIMIT);
        return channels;
    });
}
//# sourceMappingURL=props.js.map