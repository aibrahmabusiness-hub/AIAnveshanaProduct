// Comment out the localhost line and uncomment the render.com line when deploying to Vercel
// const API_BASE_URL = "https://anveshana-backend.onrender.com";
const API_BASE_URL = ""; // Empty string works for local testing since both run on localhost:8000
